# API
PORT=3001

# Database
# DB_PATH=./data/happypause.db

# Auth (Phase 2)
JWT_SECRET=change-this-in-production

# Password reset email (Phase 2)
# SMTP_HOST=localhost
# SMTP_PORT=1025
# SMTP_SECURE=false
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM=HappyPause <noreply@happypause.local>

# Admin: comma-separated user IDs who can approve public activities
# ADMIN_USER_IDS=uuid-1,uuid-2
